
export const fetchUserDetails = () => {
  // user details fetch
};

export const updateProfile = () => {
  // profile fields need to update
};

export const logout = () => {
  // user logout
 // get token from body and save it into blocktoken list.
};
